
# This is to ensure all graphical outputs are the same
# This code can be cut and paste after the script for the desired graphical output OR can be simply # run from here as the default option is to plot the last figure
# NB!! Remember to change the filename

ggsave("morph_eck.png", path = "figures/", dpi = 300, scale = 2)
